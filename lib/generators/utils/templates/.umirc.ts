import { defineConfig } from 'dumi';

export default defineConfig({
    title: 'utils',
    description: '工具库',
    outputPath: 'docs-dist',
    base: '/utils',
    publicPath: '/utils/',
    exportStatic: {},
});
